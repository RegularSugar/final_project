# 人机协作报告

## 一、AI 交互日志


### M1 数据处理

> **Prompt**："完成目前的 requirements.txt"、"现在只考虑 M1"、"完成以下 M1 数据加载与清洗模块"

**AI 输出摘要**：生成 `m1_data_processing.py`，包含 parquet 加载、缺失率计算、异常值检测、11 步清洗流水线、时间特征提取、3 个衍生特征（行程时长、平均速度、小费比例）。

**决策**：采用。AI 对每条清洗策略都写了注释说明理由，衍生特征也给出了含义与作用，符合作业要求。

#### 🔴 错误

**1. 路径拼接错误，导致 FileNotFoundError**

| | |
|---|---|
| **现象** | 运行 `main.py` 时报错，提示找不到 parquet 文件 |
| **原因** | AI 没有正确理解 `Path(__file__).resolve().parent.parent` 与 `data_path` 参数之间的关系，路径拼接方式导致最终指向了不存在的目录 |
| **修正** | 修正路径拼接逻辑，确保 `data_path` 参数与项目根目录正确拼接 |

**2. 任务执行顺序颠倒**

| | |
|---|---|
| **现象** | AI 将 `m1_run()` 的执行流程安排为：生成质量报告 → 数据清洗 → 保存报告。但正确的顺序应该是：生成质量报告 → 保存报告 → 数据清洗 |
| **原因** | AI 将"保存报告"望文生义地理解成了"保存清洗后的数据"，而非"保存数据质量报告"。这导致保存的报告是基于清洗后数据计算的，而非原始数据的真实质量情况，失去了质量报告的意义 |
| **修正** | 调整 `m1_run()` 中的执行顺序，将 `save_report()` 移到 `clean_data()` 之前调用 |

#### 🟡 效果未达预期

**3. 乘客数过滤条件不完整**

| | |
|---|---|
| **现象** | AI 最初仅过滤了 `passenger_count == 0` 的情况，未考虑超过合理上限（如 >5 人）的记录。黄色出租车最多容纳 4-5 名乘客，超过此数的记录同样属于异常数据 |
| **原因** | AI 对数据集领域知识（出租车容量限制）缺乏了解，只处理了最明显的"0 人"异常 |
| **修正** | 将过滤条件改为 `(passenger_count == 0) \| (passenger_count > 5)`，完整覆盖无效乘客数 |


---

### M2 可视化

> **Prompt**："完成以下 3 项分析，每项对应至少 1 张图表"、"再选一项最有洞察价值的分析"、"m2_5 主标题调大一点"

**AI 输出摘要**：生成 6 张图表：需求时间对比、区域热度 TOP10 + 热力图、车费因素散点+箱线图、地理空间地图、费率类型分析。

**决策**：采用。图表设计合理，中文字体配置正确，子图布局清晰。

#### 🔴 错误

**1. 图表命名不符合作业规范**

| | |
|---|---|
| **现象** | AI 将自选分析（费率类型分析）命名为 `m2_5_ratecode_analysis.png`，而作业要求命名格式为 `m2_{1~4}_{自定义}.png`，自选分析应为 m2_4 开头 |
| **原因** | AI 没有严格遵循作业文档中的编号规则，自行扩展了编号，将地理空间加分项和自选分析分别编号 m2_4 和 m2_5 |
| **修正** | 将费率分析改为 `m2_4_ratecode_analysis.png`，同步更新 `m4_qa_system.py` 中的 `chart_map` 引用和 `outputs/` 目录中的文件名 |

#### 🟡 效果未达预期

**2. 地理空间地图显示范围太小**

| | |
|---|---|
| **现象** | `m2_4_geospatial_map.png` 只显示了一小块区域，核心区域（曼哈顿）没有放大，可读性差 |
| **原因** | AI 没有根据数据分布自动调整地图的显示范围，默认绘制了整个 NYC 区域，但热点集中在曼哈顿附近 |
| **修正** | 调整地图的 xlim/ylim 范围，聚焦曼哈顿核心区域，使上下客热点区域更清晰可见 |

**3. 费率分析图主标题不够醒目**

| | |
|---|---|
| **现象** | `m2_5_ratecode_analysis.png` 的主标题字号偏小（16），在多子图布局中不够突出 |
| **原因** | AI 对多子图场景下的标题层级考虑不足，默认字号在 4 子图布局中显得不够醒目 |
| **修正** | 将主标题字号从 16 调整为 20，并加粗，使整体标题层级更清晰 |


---

### M3 预测模型

> **Prompt**："用 PyTorch 构建神经网络，预测某区域某时段的出行需求量，并与随机森林对比"

**AI 输出摘要**：生成完整建模流程：样本构造（按区域-小时-日期聚合）、Embedding+MLP 神经网络、随机森林对比、Loss 曲线、MAE/RMSE 指标 CSV。

**决策**：采用。特征设计有详细注释，模型架构合理，对比实验完整。

#### 🟢 代码质量问题

**1. `__init__` 中属性被重复赋值 6 次**

| | |
|---|---|
| **现象** | `self.label_encoder`、`self.feature_cols`、`self.num_zones`、`self.nn_model`、`self.rf_model` 五个属性在 `__init__` 末尾被连续赋值了 6 次（共 30 行完全相同的代码），触发静态分析警告 |
| **原因** | AI 在迭代修改代码时，多次追加了相同的初始化语句而未清理旧代码 |
| **修正** | 删除多余的重复赋值，每个属性只保留首次赋值 |

**2. 类型标注缺失，触发大量静态分析警告**

| | |
|---|---|
| **现象** | 多处 `np.ndarray` 类型被误报为 `Sized` 类型不匹配、`np.min(y)` 返回值被警告为 `floating` 类型、部分变量缺少类型标注（如 `np.ndarray \| None`） |
| **原因** | AI 在生成代码时未充分添加类型标注，在严格的类型检查器下会触发警告 |
| **修正** | 为关键属性添加 `np.ndarray \| None` 类型标注，为 `np.min()` 等返回值添加 `float()` 转换 |


---

### M4 问答系统

> **Prompt**："通过 .py 文件实现命令行问答循环，支持至少 5 种问题类型"、"使用 Gradio 可视化界面"、"接入大模型 API"

**AI 输出摘要**：生成完整问答系统——6 种意图识别（关键词匹配+正则提取）、结构化回答（数字结论+文本解释+图表路径）、Gradio 界面（聊天面板+快速提问+LLM 设置）、LLM 兜底（兼容 DeepSeek/Qwen/GLM）。

**决策**：采用。功能完整，交互体验好。

#### 🔴 错误

**1. 端口冲突导致启动失败**

| | |
|---|---|
| **现象** | 运行 `launch()` 时报错 `OSError: Cannot find empty port in range: 7860-7860`，7860 端口已被其他进程占用 |
| **原因** | AI 固定使用 7860 端口，未考虑端口被占用的常见场景，缺乏容错处理 |
| **修正** | 改为循环尝试 7860-7864 端口，全部失败则由系统自动分配（`server_port=0`），确保总能启动 |

#### 🟡 效果未达预期

**2. LLM 设置面板默认折叠**

| | |
|---|---|
| **现象** | AI 使用 `gr.Accordion` 组件包裹 LLM 设置面板，导致界面默认折叠，用户需要手动点击展开才能看到 API 配置项 |
| **原因** | AI 从"界面整洁"角度选择了折叠设计，但用户希望设置面板始终可见、方便填写 |
| **修正** | 移除 `gr.Accordion` 包裹，改为直接使用 `gr.Column` + `gr.Markdown` 标题，使设置面板始终展开 |

> 用户反馈："我的意思不是要折叠，就是不要有折叠功能，一直打开。"

#### 🟢 代码质量问题

**3. 快速提问按钮存在闭包变量隐患**

| | |
|---|---|
| **现象** | 代码中使用 `lambda q=q: q` 为循环中的快速提问按钮创建回调函数，静态分析工具提示"变量 'q' 在循环中可能导致意外行为" |
| **原因** | Python 闭包延迟绑定机制下，lambda 中的 `q` 默认引用循环变量而非当前值，虽然用默认参数 `q=q` 做了捕获，但写法不够清晰，容易引发误解 |
| **修正** | 改为工厂函数 `make_click_handler(text)` 显式绑定参数，代码意图更清晰，消除静态分析警告 |

**4. 大量静态分析警告（共 59 个）**

| | |
|---|---|
| **现象** | 包括：未使用的 import（`Optional`）、多处 `entities.get()` 返回值缺少类型标注、`mask.sum()` 返回类型不明确、重复的 CSS 字符串等 |
| **原因** | AI 在快速迭代中产生了未清理的 import、不完整的类型标注和重复代码 |
| **修正** | 系统性清理：移除未使用的 import、为所有 `entities.get()` 返回值添加类型标注（如 `hour: int \| None`）、提取重复 CSS 为模块级常量、为 `_extract_*_params` 方法添加 `@staticmethod` 装饰器并提取公共的 `_parse_hour()` 方法消除重复代码 |

---

## 二、三阶段对比——以 M1 数据缺失率统计为例

选取 **M1 数据处理中的"计算各列缺失率并生成统计表"** 作为对比案例。该功能接收一个 DataFrame，统计每列的缺失数量与缺失率，输出一个包含列名、总数、缺失数、缺失率的统计表。

### Native 版（独立手写）

```python
def compute_missing_stats(df):
    total = len(df)
    result = []
    for col in df.columns:
        miss = df[col].isnull().sum()
        rate = round(miss / total * 100, 2)
        result.append([col, total, miss, rate])
    return pd.DataFrame(result, columns=["列名", "总数", "缺失数", "缺失率(%)"])
```

**特点**：
- 效率较低
- 代码问题：逐列循环效率低，返回类型不明确，代码风格偏过程式

### Prompt 版（AI 辅助，给出明确指令）

向 AI 提供以下 Prompt：
> "写一个 Python 函数，参数是 pd.DataFrame，返回一个 pd.DataFrame。统计每列的缺失数和缺失率（百分比，保留两位小数），返回的 DataFrame 包含四列：列名、总数、缺失数、缺失率(%)。要求用 pandas 向量化操作，加上类型标注和 docstring。"

AI 输出：

```python
def compute_missing_stats(self) -> pd.DataFrame:
    """计算各列缺失率统计"""
    total = len(self.df)
    missing_count = self.df.isnull().sum()
    missing_rate = (missing_count / total * 100).round(2)
    stats = pd.DataFrame({
        "列名": self.df.columns,
        "总数": total,
        "缺失数": missing_count.values,
        "缺失率(%)": missing_rate.values,
    })
    return stats.reset_index(drop=True)
```

**特点**：
- 理解深度：Prompt 中需要明确指定输出格式、精度要求、向量化操作，需要对 pandas 有一定理解
- 优势：代码结构清晰，有类型标注和文档字符串，向量化操作比逐列循环更高效

### Vibe 版（自然语言对话驱动）

对话过程：
> 用户："M1任务主要做数据质量报告，先帮我算一下每列有多少缺失值"

> AI："可以用 `df.isnull().sum()` 来统计，我写一个函数输出统计结果"

> 用户："能不能也显示缺失率？用百分比"

> AI："好，我加上缺失率，顺便整理成 DataFrame 方便后续保存"

> 用户："那你把异常值也加上吧，比如距离为负的、费用为 0 的"

> AI："可以，我加一个异常值统计，包括负值、零值、超出合理范围的记录数"

最终产物：同时包含缺失率统计和异常值统计的完整质量报告函数，代码见 `m1_data_processing.py` 的 `compute_missing_stats` 和 `compute_outlier_stats` 方法。

**特点**：
- 效率：多轮对话逐步完善，不如prompt版本效率高，但是对动脑要求低（prompt版本需要用户自己先完整思考需求）
- 理解深度：最深。通过对话逐步发现需求盲区（从只看缺失值 → 加缺失率 → 加异常值统计），Vibe 版最终产出的质量报告比 Native 和 Prompt 版覆盖更全面
- 优势：AI 在对话中主动建议了"异常值统计"这个需求盲区，用户自己没有一开始想到

### 三版对比总结

| 维度 | Native 版 | Prompt 版 | Vibe 版 |
|------|-----------|-----------|---------|
| 代码行数 | ~10 行 | ~20 行 | ~40 行（含异常值统计） |
| 功能完整度 | 仅缺失率 | 缺失率 + 文档 | 缺失率 + 异常值 + 文档 |
| 理解深度 | 浅 | 中 | 深 |
| 适用场景 | 快速验证 | 需求明确时 | 需求模糊时逐步探索 |

**结论**：Vibe 版虽然耗时最长，但产出的功能最完整，且 AI 在对话中主动补充了用户没想到的需求点。对于需求明确的任务，Prompt 版效率最高；对于需求模糊的任务，Vibe 版能帮助发现盲区。

---

## 三、反思

完成本次作业后，我对 AI 工具的能力边界有了更清晰的认识。

**AI 能做好的事**：生成标准化的代码框架（如类结构、函数签名、文档字符串）、处理常见的编程模式（如数据清洗流水线、matplotlib 绑图）、快速修复静态分析警告。这些任务的共同点是"模式明确、有大量训练数据"，AI 性能出众，效率非常高。

**AI 容易出错的事**：理解项目级别的命名规范（如 M2 图表编号）、预判运行环境问题（如端口冲突）、处理 UI/UX 细节（如折叠面板的用户体验）。这些错误往往不是"代码写错了"，而是"没理解上下文"——AI 不知道作业要求 m2_4 是什么、不知道 7860 端口可能被占用、不知道用户具体喜好。

**我最深刻的体会**：AI 像是一个"能力很强但不了解项目背景的新同事"。它能写出语法正确、结构清晰的代码，但需要人来把关方向、检查规范、验证边界。最好的协作方式不是"把任务丢给 AI"，而是"人做决策、AI 做执行"——人负责定义需求、审查输出、修正方向；AI 负责生成代码、修复警告、提供备选方案。

**能力边界总结**：AI 擅长"怎么做"，不擅长"做什么"和"为什么"。未来使用 AI 时，我会更注重在 Prompt 中提供充分的上下文约束（命名规范、输出格式、边界条件），而不是期望 AI 自行推断这些隐含需求。